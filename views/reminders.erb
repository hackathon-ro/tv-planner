<%= erb :_header, :locals => { :title => "Reminders"} %>

<div class="span8">
  <h2>Reminders</h2>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Name</th>
        <th>Episod_no</th>
        <th>Airdate</th>
      </tr>
    </thead>
    <tbody>
      <% @alerts.each do |item| %>
        <tr>
          <td> <%= item.name %> </td>
          <td> <%= item.episode_nr %> </td>
          <td> <%= item.airdate_pretty %> </td>
          <td>
            <a href="/mark_seen?id=<%= item.id %>"> <i class="icon-ok"> </i> </a> 
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<%= erb :_footer %>